# Backend environment variables for local development
# NOTE: Do NOT commit real secrets to source control. Add .env to .gitignore.

# Database (individual components)
DB_HOST=localhost
DB_USER=root
DB_PASSWORD=root12
DB_NAME=study_buddy_db
DB_DIALECT=mysql

# Or provide a full connection string (mysql):
# DATABASE_URL=mysql://user:password@host:3306/dbname
DATABASE_URL=

# App
PORT=5000
NODE_ENV=development
